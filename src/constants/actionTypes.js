const prefix = '@@redux-state-restore';

const SAVE = `${prefix}/SAVE`;
const REMOVE = `${prefix}/REMOVE`;
const LOAD = `${prefix}/LOAD`;
const REPORT_ERROR = `${prefix}/REPORT_ERROR`;

export {
    SAVE,
    REMOVE,
    LOAD,
    REPORT_ERROR,
}
